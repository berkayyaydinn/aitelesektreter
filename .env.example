# Kök referans .env — gerçek değerler her servisin kendi .env'inde.
# backend/.env  ve  voice-agent/.env  dosyalarını ayrıca doldur.

# Ortak iç servis kimlik doğrulama (voice-agent <-> backend)
INTERNAL_API_KEY=change-me-shared-secret
