# LiveKit Agents ses hattı (1.x: prewarm, preemptive_generation, turn_detection)
livekit-agents>=1.0
livekit-plugins-silero>=1.0        # VAD
livekit-plugins-turn-detector>=1.0 # model tabanlı sıra tespiti (TURN_DETECTION=multilingual)

# Swappable provider eklentileri (.env ile seçilir; sadece kullanılanı kur)
livekit-plugins-deepgram>=0.6      # STT (Türkçe)
livekit-plugins-openai>=0.10       # LLM (GPT-4o-mini) + alternatif STT/TTS
livekit-plugins-azure>=0.5         # STT/TTS (Azure Neural Türkçe)
livekit-plugins-elevenlabs>=0.7    # TTS alternatif

# Backend internal API istemcisi
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7
